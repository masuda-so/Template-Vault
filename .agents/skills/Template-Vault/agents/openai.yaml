interface:
  display_name: "Template Vault"
  short_description: "Repo-specific patterns and workflows for Template-Vault"
  default_prompt: "Use the Template-Vault repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true